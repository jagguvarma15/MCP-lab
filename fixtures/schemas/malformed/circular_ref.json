{
  "name": "circular_ref_tool",
  "description": "A tool with a schema that references itself. Tests parser resilience against infinite recursion.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "data": {
        "type": "string",
        "description": "Some input data"
      },
      "nested": {
        "$ref": "#/inputSchema",
        "description": "Recursive reference back to the root schema"
      }
    },
    "required": ["data"]
  }
}
